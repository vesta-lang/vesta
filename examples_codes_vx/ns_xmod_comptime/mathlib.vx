// NS.2/MC cross-modulo: funcion comptime + macro en un namespace, usadas por su
// nombre cualificado desde OTRO modulo.  Se pliegan/expanden en compile-time.
namespace mat;

public comptime i64 sq(i64 n) { return n * n; }

@Macro
	public comptime string veces7(i64 n) { return to_str(n * 7); }
