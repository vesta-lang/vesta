// Bench: Fibonacci recursivo profundo.
// Stress test del frame stack + CALL overhead.
// fib(30) = 832040, hace ~2.7M llamadas recursivas.

i32 fib(i32 n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

i32 main() {
    return fib(30);
}
