// @vex-snippet: vex_header_prelude
// @vex-requires:
// @vex-includes: stdint.h
// @vex-freestanding-skip: no

/* Prelude del header C publico generado por `vex --emit-header`.
 * Contiene lo minimo que un consumidor C necesita: tipos de ancho fijo y la
 * apertura de `extern "C"` para que el header sea incluible desde C y C++.
 * El include guard y los typedefs/prototipos los emite el generador
 * (data-driven); este snippet es la parte ESTATICA, editable por el usuario. */

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif
